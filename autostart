###################################################################   #
# This is the current autostart file I use in my labwc installation.   #
# pipewire, pipewire-pulse & wireplumber were started as dinit        #
# user service for Artix Linux (dinit). Google AI was used to aid the #
# the configuration.                                                   #
#######################################################################

# for notification
swaync &

# start the notifier script
sleep 3 && ~/scripts/music_notifier.sh &

# waybar

waybar --log-level off &

# This puts udiskie in the waybar tray. This also sends out a
# notification when a drive is mounted/unmounted.

udiskie --tray --event-hook '~/.config/udiskie/notify.sh %device_file' &

# The next 2 lines starts the network and bluetooth applets. sleep
# used to delay to make sure waybar is settled before the applets
# start

sleep 3 && nm-applet --indicator &
sleep 4 && blueman-applet &

# Start the swww-daemon with --no-cache option to prevent artifacts
# from the previous wallpaper to seen in the next.

swww-daemon --no-cache &
# Start the shell script wrapper for swww to use random transitions
# for all the *.jpg & *.png images in my Wallpapers directory
 
~/scripts/wallpaper.sh &





